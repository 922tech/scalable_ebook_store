from django.urls import path, re_path


urlpatterns = [
    # path(r'books/register/', UploadView.as_view(), name='tus_upload'),
    # path(r'books/register/<str:resource_id>', UploadView.as_view(), name='tus_upload_chunks'),
]
